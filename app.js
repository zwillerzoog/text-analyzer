"use strict";

$(document).ready(function() {
	
});

$(".button").click(function(event) {
	$(".hidden text-report").show();
});

/*function makeArray() {
	var text = $(".textInput").find().text()
}

function analyze() {
	var textArray = [input];
	var wordCount = textArray.length;
	/*var uniqueCount = (Words in the array that have a count of 1);*/
/*	var characterSum = 
	var averageLength = wordCount / characterSum;
}
/*console.log("Hello world");

var input = $("textarea");

